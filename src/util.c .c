// # Utility functions like hashing, encoding, etc.

#include "../include/util.c .h"

void createDatabase(const char *databaseName) {
    // Concatenate the `data` directory path with the `databaseName`
    // to form the path for the new database directory
    // Use filesystem APIs to create the directory
}


void createTable(const char *databaseName, const char *tableName) {
    // Determine the correct directory path for the database
    // Create the table file within this directory
}

